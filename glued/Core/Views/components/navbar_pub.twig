        <ul class="navbar-nav navbar-right">
          <li class="dropdown">
            <a href="{{ url_for('app.core.auth.callback') }}" class="btn btn-primary btn-block btn-icon-split" onClick='deleteCookie()'>{{ __('Sign in') }}</a>
            <a href="{{ url_for('app.core.auth.signout') }}" class="btn btn-primary btn-block btn-icon-split">{{ __('Sign out') }}</a>
          </li>
        </ul>
