        <ul class="navbar-nav navbar-right">
          <li class="dropdown">
            <a href="{{ url_for('core.auth.signup') }}" class="btn btn-primary btn-block btn-icon-split">{{ __('Sign up') }}</a>
          </li>
          <li class="dropdown">
            <a href="{{ url_for('core.auth.signin') }}" class="btn btn-primary btn-block btn-icon-split">{{ __('Sign in') }}</a>
          </li>
        </ul>
